# docs/ Full-Coverage Audit

**Scope:** all Markdown files under `docs/` (`find docs -name '*.md' | sort`)
**File count:** 150
**Total LOC:** 23,238 (`find docs -name '*.md' -exec cat {} + | wc -l`)

**Per-subdirectory file counts** (`find docs -name '*.md' | awk -F/ '{print $1"/"$2}' | sort | uniq -c | sort -rn`):

```
     64 docs/archived-docs
     46 docs/backend
     25 docs/audits
      3 docs/operations
      3 docs/architecture
      2 docs/platform
      2 docs/frontend
      1 docs/templates
      1 docs/platform_backtest_output_audit.md   (root file)
      1 docs/implementation-summaries
      1 docs/frontend_audit_and_roadmap.md        (root file)
      1 docs/README.md                            (root file)
```

Status: IN PROGRESS — populated incrementally, ~15 files per batch, per task instructions (prior attempts died mid-run).

---

## Per-file notes

### Root / architecture / archived-docs (contracts) — batch 1

- docs/README.md — documentation index / placement rules for the docs tree. ⚠ STALE: references `docs/roadmaps/` and `docs/unsorted/`, neither of which exists (verified via `ls`).
- docs/architecture/data-flow.md — current end-to-end runtime data flow (ingestion→storage→strategy→safety→execution→reconciliation→portfolio→risk→audit); explicitly marks itself as reflecting implemented behavior with a "Deferred Flow Components" section. No stale refs found (prose-level, no concrete paths/commands).
- docs/architecture/layering.md — describes the layered architecture (contracts→domain services→storage→orchestration→interfaces) and current boundary deviations. No concrete repo paths cited to verify; consistent with CLAUDE.md's layering description.
- docs/architecture/system-overview.md — platform summary, runtime shape, persistence/safety model, known gaps. Non-concrete, no stale refs.
- docs/archived-docs/compile-vs-runtime.md — defines compile-time (frozen inputs) vs runtime (stateful) semantics for a run. Planning-level spec, no repo paths.
- docs/archived-docs/contracts/broker-order.md — BrokerOrder contract spec (schema, invariants, state machine). Planning-level v1 spec doc, part of archived canon.
- docs/archived-docs/contracts/cash-snapshot.md — CashSnapshot contract spec (schema, invariants).
- docs/archived-docs/contracts/corporate-action.md — CorporateAction contract spec (splits/dividends/mergers schema + invariants).
- docs/archived-docs/contracts/fill.md — Fill contract spec (execution event schema + invariants).
- docs/archived-docs/contracts/index.md — index/summary of all v1 data contracts (MarketBar, Signal, OrderIntent, BrokerOrder, Fill, PositionSnapshot, CashSnapshot, RiskSnapshot, CorporateAction, UniverseSnapshot, RunManifest) with producer/consumer map.
- docs/archived-docs/contracts/marketbar.md — MarketBar (5-min OHLCV bar) contract spec.
- docs/archived-docs/contracts/order-intent.md — OrderIntent contract spec (idempotency key, sizing XOR rules).
- docs/archived-docs/contracts/position-snapshot.md — PositionSnapshot contract spec (holdings schema).
- docs/archived-docs/contracts/risk-snapshot.md — RiskSnapshot contract spec (exposure/leverage/blocking schema).
- docs/archived-docs/contracts/run-manifest.md — RunManifest contract spec (immutable run config/environment record).

### archived-docs (contracts cont'd, decisions, ingestion, invariants, paper-validation) — batch 2

- docs/archived-docs/contracts/signal.md — Signal contract spec (strategy decision, direction/confidence schema).
- docs/archived-docs/contracts/universe-snapshot.md — UniverseSnapshot contract spec (time-aware membership, survivorship-safe versioning).
- docs/archived-docs/decisions.md — ADR-lite decision log (D-001..D-005: 5-min bars, Alpaca-only broker, NO_LIVE_TRADING default, Postgres SoR, Parquet datasets). Matches CLAUDE.md's stated invariants (NO_LIVE_TRADING, Postgres SoR).
- docs/archived-docs/index.md — v1 system definition/overview; links to `v1-boundaries.md`, `safety-doctrine.md`, `compile-vs-runtime.md`, `decisions.md`, `invariants.md` — all present as siblings within archived-docs (relative links resolve correctly here, unlike CLAUDE.md's top-level references to the same filenames under `docs/architecture/`, which do NOT exist there — see stale-reference table).
- docs/archived-docs/ingestion/corporate-actions-continuity.md — v1 spec: raw vs adjusted bar policy, split continuity tolerance (0.5%), dividend handling (event-only, no dividend-adjusted series in v1).
- docs/archived-docs/ingestion/incident-recording.md — v1 spec: ingestion incident event types/fields (INGESTION_SLA_MISSED, MARKETBAR_OUTLIER_DETECTED, etc.) and required fields.
- docs/archived-docs/ingestion/index.md — index for ingestion pipeline spec folder (sources, SLAs, outliers, corp-action continuity, incident recording); all 5 linked pages exist as siblings.
- docs/archived-docs/ingestion/outliers-and-missing-data.md — v1 spec: outlier thresholds (10x median spike, high<low reject, zero-volume staleness) and missing-bar forward-fill/skip/degrade policy.
- docs/archived-docs/ingestion/slas-and-fallbacks.md — v1 spec: freshness SLA (bar_close+30s target, +90s hard halt) and SKIP/DEGRADE/HALT decision tree.
- docs/archived-docs/ingestion/sources.md — v1 spec: data sources (Alpaca IEX/SIP/Blue Ocean), corp-action types, "Alpaca IEX Top 500" universe, rate-limit discipline. References "Phase 3 universe governance docs" — no such phase-numbered doc found elsewhere in docs/ (informal reference, not a broken link).
- docs/archived-docs/invariants.md — global v1 invariants (13 numbered items: NO_LIVE_TRADING default, bar alignment, idempotency, monotonic order state machine, reconciliation freeze, RunManifest requirement). This is the file CLAUDE.md calls `docs/architecture/invariants.md` — actual location is `docs/archived-docs/invariants.md`. ⚠ STALE (see CLAUDE.md cross-reference below).
- docs/archived-docs/paper-validation/index.md — index for paper-validation spec folder (operational playbook, acceptance criteria, promotion standard); all 3 linked pages exist.
- docs/archived-docs/paper-validation/operational-playbook.md — "Phase 9" spec: incident response, kill-switch test cadence, manual intervention policy, broker connectivity monitoring for paper mode.
- docs/archived-docs/paper-validation/paper-acceptance-criteria.md — "Phase 9" spec: 10-consecutive-market-day paper run requirement, zero safety-gate-violation / zero reconciliation-mismatch / idempotency-under-restart acceptance bar.
- docs/archived-docs/paper-validation/promotion-standard.md — "Phase 9" spec: end-to-end correctness/reproducibility/audit/safety proof required before v1 is declared complete.

### archived-docs (research, runtime, safety-doctrine, safety) — batch 3

- docs/archived-docs/research/backtest-semantics.md — v1 spec: bar-based simulation, fill-at-T+1 no-lookahead rule, partial-fill participation-rate formula, order auto-cancel after 1 bar.
- docs/archived-docs/research/cost-model.md — v1 linear cost model spec (commission + spread% + slippage%), VolumeShare-like slippage, determinism requirement.
- docs/archived-docs/research/experiment-tracking.md — required backtest output artifacts (run_manifest.json, metrics_summary.json, trades_journal.parquet, debug_report.json) and required metrics list.
- docs/archived-docs/research/fill-model.md — detailed fill-model spec incl. R-07 gap-open/intrabar-touch limit fill logic, F-01 probabilistic partial fills, F-02 stochastic order rejection, DAY-expiry semantics. Notably more detailed/updated than sibling docs (references specific rule IDs R-04/R-07/F-01/F-02), suggesting this file was updated post-archival while neighbors were not — worth flagging as inconsistent freshness within the "archived" folder rather than a stale reference per se.
- docs/archived-docs/research/index.md — index for research engine spec folder; links to 5 sibling docs, all present.
- docs/archived-docs/research/research-engine-overview.md — deterministic research-engine spec: shared contracts with live/paper, 9-step deterministic evaluation cycle, no-lookahead invariants.
- docs/archived-docs/research/stress-test-spec.md — required stress-test scenarios (gap shock, vol spike, data outage, delayed bars, extreme slippage, liquidity collapse) and acceptance criteria.
- docs/archived-docs/runtime/index.md — index for runtime spec folder (lifecycle, scheduler, order state machine, strategy state machine, reconciliation); links to 5 sibling docs, all present.
- docs/archived-docs/runtime/lifecycle-semantics.md — v1 runtime lifecycle spec: 9-step cycle, global invariants, freeze conditions, event-recording contract (event_id/type/entity/hash_prev).
- docs/archived-docs/runtime/order-state-machine.md — self-marked "(archived — superseded by E-01/E-02)"; explicitly states canonical reference is `docs/architecture/broker_event_stream_and_order_lifecycle.md`. ⚠ STALE: that path does not exist — actual file is `docs/backend/broker/broker_event_stream_and_order_lifecycle.md` (verified via `ls`). The doc itself is otherwise self-aware/correctly labeled as historical.
- docs/archived-docs/runtime/reconciliation.md — v1 reconciliation spec: runs pre-eval/EOD/on-restart, mismatch → freeze + human-ack required, required reconciliation events.
- docs/archived-docs/runtime/scheduler-model.md — v1 scheduler spec: UTC/NYSE time model, 30s ingestion SLA / 35s eval-start deadline, SLA-miss decision tree, required per-cycle event sequence (BAR_CLOSED...CYCLE_COMPLETED).
- docs/archived-docs/runtime/strategy-state-machine.md — v1 strategy lifecycle spec (IDLE→SIGNALLED→PENDING→IN_POSITION→EXIT_PENDING→COOLDOWN→IDLE), forbidden transitions, event mapping table.
- docs/archived-docs/safety-doctrine.md — capital-protection non-negotiables (env isolation, NO_LIVE_TRADING default, 4-layer live-enablement gates, broker allowlist, exposure caps, idempotency, kill switch, shadow mode, mandatory reconciliation). This is the file CLAUDE.md calls `docs/architecture/safety-doctrine.md` — actual location is `docs/archived-docs/safety-doctrine.md`. ⚠ STALE (see cross-reference table).
- docs/archived-docs/safety/broker-account-allowlist.md — broker account allowlist spec: environment-scoped allowlists, allowlist must live outside primary DB, acceptance tests.

### archived-docs (safety cont'd, storage, universe) — batch 4

- docs/archived-docs/safety/caps-and-throttles.md — hard caps spec: exposure/notional caps, order-rate limits, shadow-mode contract, broker allowlist recap.
- docs/archived-docs/safety/environment-model.md — paper vs live environment isolation spec: separate namespaces/credentials/DB partitions, "paper-only build" concept (live adapter excluded from build artifact).
- docs/archived-docs/safety/idempotency-and-dedupe.md — idempotency key spec: deterministic hash of (run_id, strategy_id, bar_timestamp, symbol, side, target_qty), pre-broker persistence requirement.
- docs/archived-docs/safety/index.md — index for safety spec folder; links to 6 sibling docs (safety-invariants, environment-model, layered-enablement-gates, broker-account-allowlist, caps-and-throttles, idempotency-and-dedupe, shadow-mode — 7 listed), all present.
- docs/archived-docs/safety/layered-enablement-gates.md — 4-gate live-enablement spec: build-time gate (ENABLE_LIVE_BUILD), config gate (NO_LIVE_TRADING), runtime human-confirmation token gate, external kill switch gate.
- docs/archived-docs/safety/safety-invariants.md — condensed 7-point safety invariant list (env isolation, 4-layer live gating, kill switch outside DB/service, pre-broker cap enforcement, shadow-mode broker-disabled guarantee).
- docs/archived-docs/safety/shadow-mode.md — shadow mode spec: what runs (ingestion/universe/strategy/signals/OrderIntent/risk/audit) vs what must never run (any broker call), required/prohibited output artifacts, acceptance tests.
- docs/archived-docs/storage/audit-log.md — append-only audit event log spec: canonical event envelope (event_id, run_id, sequence_num, hash chaining), event type taxonomy (run/step/order/broker/reconciliation lifecycle events), retention policy. Contains leftover AI-generation citation artifacts (`:contentReference[oaicite:...]{index=...}`) embedded in prose — a documentation-quality defect, not a broken link.
- docs/archived-docs/storage/dataset-version.md — DatasetVersion contract spec: immutable dataset metadata, checksum spec, lineage JSON structure. Also contains leftover `:contentReference[oaicite:...]` citation artifacts.
- docs/archived-docs/storage/index.md — index for storage layer spec folder. ⚠ STALE (internal, minor): body text references sibling files `dataset-versioning.md` and `universe-versioning.md`, but the actual filenames in this same folder are `dataset-version.md` and `universe-version.md` (verified — no `-versioning.md` files exist). Also references `docs/contracts/run-manifest.md` etc. as literal paths — these resolve only relative to `docs/archived-docs/contracts/`, not a top-level `docs/contracts/` (which does not exist). Also contains citation artifacts.
- docs/archived-docs/storage/parquet-datasets.md — Parquet dataset conventions: `bars_raw_5m`/`bars_adj_5m`/`corporate_actions`/`universe_membership` dataset families, `data/<name>/version=<id>/symbol=.../date=.../` partition scheme. Contains citation artifacts.
- docs/archived-docs/storage/postgres-system-of-record.md — Postgres SoR planning spec: 10-table inventory (runs, run_manifest, orders, broker_order_events, fills, position/cash/risk_snapshots, universe_snapshots, dataset_versions) with conceptual fields/invariants. Contains citation artifacts. This is the earliest planning-level ancestor of the current `storage/sor/` implementation described in CLAUDE.md.
- docs/archived-docs/storage/universe-version.md — UniverseVersion contract spec: membership hash spec (normalize/sort/hash), selection-criteria JSON shape. Contains citation artifacts.
- docs/archived-docs/universe/index.md — index for universe governance spec folder; links to v1-universe-spec, universe-invariants, survivorship-controls, universe-lifecycle — all present as siblings.
- docs/archived-docs/universe/survivorship-controls.md — survivorship-bias control spec: time-aware UniverseSnapshot lookup rule (closest preceding snapshot ≤ T), prohibits "current universe" use in historical runs.

### archived-docs (universe cont'd, v1-boundaries, vertical-slice) — batch 5

- docs/archived-docs/universe/universe-invariants.md — condensed universe invariants (immutability, versioning, RunManifest linkage, pure-function selection).
- docs/archived-docs/universe/universe-lifecycle.md — symbol lifecycle spec: SymbolMapping table for delistings/mergers/ticker changes, rule that historical bars stay under original symbol.
- docs/archived-docs/universe/v1-universe-spec.md — locked v1 universe definition: "v1_iex_top500_liquid", $5 price floor, $10M ADV liquidity filter, monthly rebalance, explicit ETF/ADR/OTC/SPAC exclusions.
- docs/archived-docs/v1-boundaries.md — locked v1 scope doc: 5-min bars/Alpaca-only/single strategy/single universe/paper-first, explicit non-goals, "v1 done" definition. This is the file CLAUDE.md calls `docs/architecture/v1-boundaries.md` — actual location is `docs/archived-docs/v1-boundaries.md`. ⚠ STALE (see cross-reference table).
- docs/archived-docs/vertical-slice/execution-constraints.md — "Phase 8" spec: market/limit orders only, DAY TIF only, extended-hours off by default, fractional trading allowed.
- docs/archived-docs/vertical-slice/failure-semantics.md — "Phase 8" spec: missing-data skip (no forward-fill for signals), broker-rejection→COOLDOWN, capital-breach→no OrderIntent, kill-switch→full liquidation.
- docs/archived-docs/vertical-slice/index.md — index for vertical-slice spec folder; links to strategy-spec, execution-constraints, failure-semantics — all present.
- docs/archived-docs/vertical-slice/strategy-spec.md — "Phase 8" spec: SMA_Cross_v1 baseline strategy (20/50 SMA cross, long-only, 5% capital per position, max 5 concurrent positions, 3-bar flip-suppression cooldown, stop-loss disabled in v1).

### docs/audits — CLI domain audits (batch 5, 2 of 17 in "cli separate domain audits" subfolder)

- docs/audits/agent-findings/cli separate domain audits/admin_cli_domain_audit.md — audit of `admin` CLI domain (`cli/commands/admin.py`); status COMPLETE, 54 passing tests; documents 8 read-only admin commands (inspect-config/env/failed-runs/audit-log/db, validate-config, doctor) plus a `runtime list-failed-runs` alias; notes one deferred P2 item (`admin export-failure-bundle`).
- docs/audits/agent-findings/cli separate domain audits/backtesting_cli_domain_audit.md — audit of deprecated `backtesting` CLI domain; concludes nearly all 14 commands are mis-homed (settings/controls/portfolio/risk/operations/governance utilities masquerading under `backtesting`), proposes a full migration map to `platform backtest`, `settings`, `controls`, `portfolio`, `risk`, `operations`, `governance`; flags `backtesting run`/`inspect-results` as unimplemented placeholders and `_GOVERNANCE_SETTING_WIRING` in `backtesting.py` as stale relative to current `AutoPromotionService`/`AutoDemotionService` behavior.

### docs/audits — CLI domain audits (batch 6, 6 more of 17)

- docs/audits/agent-findings/cli separate domain audits/controls_cli_controls_py_domain_audit.md — `controls.py` does not exist yet / not registered in `cli/main.py`; audit catalogs control-like commands scattered in `backtesting`/`safety` and proposes a full `controls` domain (state/pause/resume/mode/strategy toggle/allocation override commands) sourced from existing `RuntimeControlService`/`StrategyControlService`/`StrategyAllocationService`; flags that `backtesting seed-controls --clean` deletes governance/control/allocation rows with no dry-run and that `RuntimeControlService.resume_trading()` can accidentally clear an active kill switch.
- docs/audits/agent-findings/cli separate domain audits/diagnostics_cli_domain_audit.md — audit of `diagnostics` domain (`cli/commands/diagnostics.py`); only command is `diagnostics snapshot`, which is read-only-intended but can silently call broker APIs via `AlpacaPortfolioService` (no `--local-only`/`--include-broker` flag); proposes section filters, output-artifact support, and dedicated CLI tests (currently none exist for this file).
- docs/audits/agent-findings/cli separate domain audits/execution_cli_domain_audit.md — audit of `execution` domain (5 commands); flags `execution reconcile-open-orders` as **broken**: parser wires `func=handle_reconcile_open_orders` but the handler signature requires `(_args, run_id)` while `run_handler` only passes `args`, and the parser has no `--run-id` option, so invocation fails. Also notes `reconcile-order` has no `--dry-run`/live-gate visibility despite broker-mutating behavior, and that broker-runtime-sync/external-reconciliation/fill-quality/execution-policy-preview services exist but have zero CLI coverage.
- docs/audits/agent-findings/cli separate domain audits/features_cli_domain_audit.md — audit of `features` domain (1 command, `features run-pipeline`); flags a real bug: all `--include-*` flags use `action="store_true", default=True`, so passing or omitting them both yield `True` — the CLI cannot actually disable returns/volatility/moving-average/liquidity/regime feature jobs even though the underlying cycle supports `False`. Also notes `--price-basis ADJUSTED` is accepted but the resolver always reads `RAW_BARS_DATASET` regardless, and no `--dry-run`/JSON output exists.
- docs/audits/agent-findings/cli separate domain audits/governance_cli_governance_py_domain_audit.md — `governance.py` does not exist yet; governance behavior (promotion/demotion verification) currently lives under `backtesting`. Important correction of a claim made in the sibling `backtesting_cli_domain_audit.md`/`cli_documentation_drift_audit.md`: this audit confirms `auto_promote_enabled` and `auto_demote_on_breach` **are** actually wired (verified at specific line numbers in `AutoPromotionService.run()` / `AutoDemotionService.run()`), and that the `_GOVERNANCE_SETTING_WIRING` table inside `backtesting.py` showing `FLAG_NOT_WIRED` for these is itself stale/wrong.
- docs/audits/agent-findings/cli separate domain audits/ingestion_cli_domain_audit.md — audit of `ingestion` domain (4 commands: run-bars, run-backfill, run-corporate-actions, inspect-bar); all mutating commands lack `--dry-run`, date/range/symbol-count validation, and structured output (print only `"success"`, no run IDs/dataset version IDs returned); `run-backfill` has no guard against `--end` before `--start` or unbounded historical ranges; `inspect-bar` needlessly commits a UOW transaction for a read-only lookup.

### docs/audits — CLI domain audits (batch 7, final 6 of 17)

- docs/audits/agent-findings/cli separate domain audits/operations_cli_domain_audit.md — audit of `operations` domain (1 command, `verify-runtime-soak`); flags that the command sounds read-only but `persist_report=True` by default, writing a `runtime_soak_reports` row every call with no `--dry-run`/`--no-persist` flag; `--stale-after-minutes` has no bounds validation; WARNING-severity reports exit 0 (only FAILED exits 1).
- docs/audits/agent-findings/cli separate domain audits/portfolio_cli_portfolio_py_domain_audit.md — `portfolio.py` does not exist yet / not registered; only portfolio-like surface today is `backtesting read-portfolio`. Proposes a large read-only `portfolio` domain (summary, holdings, allocation, equity-curve, performance, cash/positions, construction-pipeline artifacts). Flags `PortfolioConstructionRepository._persist_batch_items()` as a placeholder `pass` (raw signal persistence requires an explicit separate call), and that REST portfolio summary/holdings/allocation can silently call Alpaca when not in simulation mode.
- docs/audits/agent-findings/cli separate domain audits/research_cli_domain_audit.md — audit of `research` domain (11 commands); flags `research resume-experiment` as **misleading**: it never resumes work, it just calls `handle_plan_restart` internally and emits a restart plan (its `--dry-run` flag is effectively always true / has no effect). Notes strategy/component catalog commands (`list-strategy-types`, `inspect-strategy`, etc.) duplicate what should be canonically owned by a `strategy` domain, and there's no CLI coverage for experiment listing/detail/cancellation despite services existing.
- docs/audits/agent-findings/cli separate domain audits/risk_cli_risk_py_domain_audit.md — `risk.py` does not exist yet / not registered; only existing surface is `backtesting verify-risk-parameter-effects`. Proposes a full `risk` domain (limits/snapshot/exposure/concentration/pretrade-check/drawdown-ladder/risk-budget/factor commands). Documents the 5-rung drawdown ladder (NORMAL→WARNING→PROBATION→SUSPENDED→BREACHED with hysteresis/cooldown) and notes several services (`RiskBudgetingService.compute()`, `FactorExposureMonitoringService.run()`) persist as a side effect of computation with no non-persisting preview path.
- docs/audits/agent-findings/cli separate domain audits/runtime_cli_runtime_py_domain_audit.md — audit of `runtime` domain (11 commands); flags a real bug: `run-cycle` accepts `--timestamp` but `handle_run_cycle` **ignores it** and always calls `run_trading_cycle()` with no timestamp argument. Also finds `trigger-job` validates against the full scheduler registry but its dispatcher map only covers 4 of 10 registered jobs — `market_ingestion_cycle`, `feature_pipeline_cycle`, `corporate_action_ingestion_cycle`, `factor_exposure_monitoring_cycle`, `factor_neutralization_verification_cycle`, `experiment_pipeline_cycle` are registered but not dispatchable — and that 7 more scheduler cycle files exist (`run_correlation_monitoring_cycle.py`, `run_risk_budgeting_cycle.py`, `run_drawdown_governance_ladder_cycle.py`, etc.) that aren't even in `SCHEDULER_REGISTRY`, so they have no CLI trigger path at all.
- docs/audits/agent-findings/cli separate domain audits/safety_cli_safety_py_domain_audit.md — audit of `safety` domain (5 commands); flags significant wiring problems: `arm-live`/`disarm-live` construct a fresh **in-memory-only** `RuntimeGateService()` per invocation, so arming live trading in one CLI call has zero effect on a later `gate-status` call or the running scheduler process; `enable-kill-switch`/`disable-kill-switch` call `KillSwitchService` directly which flushes but never commits (question-mark persistence), and bypass the richer `RuntimeControlService.activate_kill_switch` emergency-halt path that cancels open orders and writes audit logs — meaning the CLI kill switch may not actually halt open orders the way the REST `/controls/kill-switch` endpoint does. `gate-status` also isn't strictly read-only (creates a singleton DB row on first call).

### docs/audits — CLI domain audits (batch 8, final 3 of 17 + master drift audit)

- docs/audits/agent-findings/cli separate domain audits/settings_cli_settings_py_domain_audit.md — `settings.py` does not exist yet; only surface is `backtesting seed-settings`/`read-settings`, which bypasses `OperatorSettingsService.update_settings` (writes via raw repository `setattr`, skipping the `OPERATOR_SETTINGS_UPDATED` audit-log path REST uses) and whose `_VALID_SETTINGS_KEYS` allowlist omits several real `OperatorSettingsRow` columns (`max_total_strategy_allocation_pct`, `min_rebalance_interval_hours`, drawdown-governance fields, etc). Repeats/corroborates the governance-audit finding that `auto_promote_enabled`/`auto_demote_on_breach` ARE wired despite `backtesting.py`'s stale `_GOVERNANCE_SETTING_WIRING` table saying otherwise.
- docs/audits/agent-findings/cli separate domain audits/strategy_cli_domain_audit.md — audit of `strategy` domain (2 commands); flags `strategy evaluate-bar` as **misleadingly named**: it doesn't evaluate a chosen strategy at all — it hard-wires `StubStrategy()` with manifest `strategy_id="baseline_strategy"`, reads broker account/positions/trades, and generates real order intents, so it's actually a full trading-evaluation-cycle command mis-homed under `strategy`. Also flags `strategy inspect-readiness` as domain-misaligned (checks ingestion-deadline readiness, not strategy readiness).
- docs/audits/agent-findings/cli separate domain audits/universe_cli_domain_audit.md — audit of `universe` domain (28 commands, the largest and most complete CLI domain). Flags `universe select-now` as suspicious: `run_universe_selection_cycle()` mutates ORM rows via a session with `autocommit=False` but the audit found no visible `commit()` or `session.close()`/`finally` block, meaning the command may silently not persist despite claiming to. Also notes `raw-pool-refresh`, `candidate-generate`, and `seed` all mutate/call-broker with no `--dry-run` option (unlike `propose-rebalance`/`rotate`/`rollback`, which do have dry-run).
- docs/audits/agent-findings/cli_documentation_drift_audit.md — master CLI documentation audit; ground truth is `cli/main.py` + `cli/commands/*.py`: **85 leaf commands across 12 top-level groups**. Found the pre-existing `docs/backend/cli/runtime_harness_reference.md` stale — it reported only 56 commands and significantly undercounted `universe`/`research`/`runtime`/`backtesting` coverage. This audit rebuilt `docs/backend/cli/cli.md` as canonical index and created ~10 new domain README files (`docs/backend/universe/README.md`, `docs/backend/research/README.md`, `docs/backend/execution/README.md`, etc. — all present in the current file list). Also notes `pyproject.toml` has no `[project.scripts]` entry, so the parser's `atp` prog name is not an installed console-script alias; real entrypoint is `python -m autonomous_trading_platform.cli.main`.

### docs/audits — non-CLI agent-findings (batch 9)

- docs/audits/agent-findings/feature_dependency_integration_audit.md — pre-implementation architecture audit for "TASK-2.1" (automatic strategy→feature-dataset dependency resolution). Documents 5 implemented persisted features (returns, volatility, moving_average, liquidity, regime) and finds the missing piece is narrow: a resolver translating `StrategyDefinition.required_persisted_features` into `SimulationFeatureDatasetRequest` objects, since `SimulationRunner.run()` currently always calls `load_window()` with no feature_datasets. Flags a storage-path inconsistency: `FeatureDatasetWriterService.storage_path` metadata says `feature_datasets/...` but `ParquetFeatureRepository` actually writes to `features/...`. This is a forward-looking implementation plan, not a describe-current-state doc — useful to know its recommendations may or may not have since landed.
- docs/audits/agent-findings/platform_cli_orchestration_integration_audit.md — companion to the CLI drift audit; maps all 85 commands to a domain coverage matrix and identifies that no single canonical "platform backtest run" command exists tying together universe→ingestion→features→trading→portfolio→governance→dashboard as one audited artifact bundle. Documents that several operator controls (pause/resume, trading-mode switch, strategy enable/disable, allocation override, governance transition) exist only as REST routes with **no CLI equivalent at all**. Proposes a future `platform backtest plan/run/verify/report` command family (not yet implemented — explicitly says "do not implement these yet").
- docs/audits/agent-findings/portfolio_governance_allocation_audit.md — dated 2026-05-20; deep architecture audit of governance/allocation/portfolio-construction/risk with 18 severity-ranked findings (3×P0, 8×P1/P2 mix) and a "2.7/5" portfolio-intelligence maturity score. ⚠ STALE relative to later docs in this same audit set: FINDING-01 ("No Auto-Demotion... governance FSM only promotes upward") and FINDING-03 ("Kill Switch Is In-Memory and Not Persisted... no database write, no SOR record") are both contradicted by later-dated files in `docs/audits/agent-findings/cli separate domain audits/` (governance_cli and safety_cli audits), which reference an existing `AutoDemotionService.run()` with real DB-backed logic and a `KillSwitchStateRepository`/`kill_switch_state` singleton DB row. This doc appears to predate that work landing — treat its P0 findings as historical/superseded rather than current-state facts, though FINDING-02 (no cross-strategy symbol concentration limit), FINDING-08 (no multi-strategy signal netting) are corroborated as later addressed by the "Portfolio Construction Layer" (FINDING-08 policies, `PortfolioSignalAggregator`) referenced in the portfolio_cli audit.
- docs/audits/agent-findings/research_orchestration_observability_audit.md — dated 2026-05-22; finds platform-wide observability infra (OTel+Prometheus+Tempo+Loki, `observability/lifecycle.py` helpers) is mature and well-used at the scheduler-cycle boundary (`run_experiment_pipeline_cycle`), but everything *inside* the research pipeline (stages, parallel execution, checkpoints, cache, validation, regime analysis, intelligence) is "almost entirely dark" — no metrics, no spans, minimal freeform-text logging only. Provides a coverage matrix and a phased instrumentation plan (not yet implemented — this is a plan doc, self-labeled "TASK-3.5B").
- docs/audits/agent-findings/research_strategy_audit.md — dated 2026-05-15; full architecture audit of `strategy/` and `research/`. Top finding: **research/backtest/replay semantics are split across three divergent, non-equivalent paths** — new research simulation (`SimulationRunner`), legacy `BacktestReplayOrchestrator` (inline MA logic, described as "legacy/demo"), and `BacktestTradingCycleOrchestrator` (full-fidelity). Also flags strategy-type registration duplicated across 3 places (`strategy_factory.py`, `strategy_config.py` Literal type, CLI `STRATEGY_TYPE_CHOICES`), and `SimulationRunner._record_run_started()` hard-codes `universe_version="v1"` regardless of actual universe used. Notes this doc's own path descriptions are stale relative to a (non-existent-in-current-tree) `research_execution_paths.md` referenced as superseding it.
- docs/audits/documentation_inventory_audit.md — major documentation-structure audit. ⚠ HEAVILY STALE: describes and catalogs a doc tree (`docs/domains/*.md`, `docs/orchestration/*.md`, `docs/cli/*.md`, `docs/interfaces/cli.md`, `docs/operations/runbooks.md`, `docs/operations/debugging.md`, `docs/architecture/strategy_registry.md`, `src/autonomous_trading_platform/cli/CLI_RUNTIME_HARNESS_REFERENCE.md`, `src/autonomous_trading_platform/observability/docs/instrumentation_inventory.md`, `src/autonomous_trading_platform/storage/sor/docs/template.md`, `src/autonomous_trading_platform/research/experiments/frontend_input_mapping.md`, ~40 more) that **does not exist anywhere in the current repo** (verified via direct existence checks — every single one of the ~11 spot-checked paths is missing). This entire audit predates a full documentation reorganization: the content it describes has clearly been migrated into the current `docs/backend/*`, `docs/templates/sor_template.md`, and `docs/backend/api/frontend_experiment_input_mapping.md` structure (which the `cli_documentation_drift_audit.md` and `documentation_tree_migration_summary.md` describe building). Treat this file as a historical snapshot of a since-superseded doc tree, not as a current inventory. Its proposed target structure (`docs/backend/...`) is notably close to — and possibly the design source for — the tree that exists today.
- docs/audits/execution_simulation_audit.md — deep audit of execution/simulation realism with 20 severity-tagged findings (R-01..R-07 realism, F-01..F-06 failure scenarios, D-01..D-03 mode divergence, P-01..P-03 determinism, A-01..A-03 accounting, C-01..C-02 calibration). Headline finding R-01 (Critical): simulation fills orders at the same bar close used to generate the signal ("CURRENT_CLOSE fill policy"), a structural look-ahead bias — `NEXT_OPEN` fill policy exists as an enum value but is unimplemented (`# not implemented; defer`), corroborating the fill-model gap independently noted in `docs/archived-docs/research/fill-model.md`'s "legacy behavior" section. Also finds two disconnected slippage-model systems (R-02/R-03): the more realistic `VolumeShareSlippageModelService` under `backtesting/` is fully implemented but never actually called by `SimulationRunner`/`SimulationExecutionEngine`, which use a much simpler fixed 1bps `SlippageModel` instead — a "silent model error." Note: `docs/backend/execution/execution_policy_simulation_parity.md` (TASK D-01, read later) documents that finding D-01 from this audit (execution policy engine not applied during simulation) has since been fixed — worth cross-referencing as at least one Tier-2 finding here is resolved.

## docs/backend/ — batch 11 (api, broker, cli, execution, ingestion, observability)

- docs/backend/api/contracts.md — domain overview of shared Pydantic contracts (MarketBar, CorporateAction, UniverseSnapshot, Signal→OrderIntent→BrokerOrder→Fill, snapshots, RunManifest); lists implementation gaps (schema_version handling inconsistent, append-only not enforced, RunManifest immutability not strict) consistent with archived-docs v1 spec gaps.
- docs/backend/api/frontend_experiment_input_mapping.md — documents `story-57.v1` mapping layer (`application/services/experiment_input_mapping.py`) that expands stable frontend experiment inputs (strategy_type/risk_level/time_horizon) into internal engine fields (dataset_version, universe, parameter_grid, etc).
- docs/backend/broker/broker_event_stream_and_order_lifecycle.md — current (E-01/E-02) broker order lifecycle spec: 9-state machine (adds PENDING_NEW/PENDING_CANCEL/EXPIRED vs the old 6-state v1 spec), websocket stream + polling reconciliation dual-path with idempotent delta-qty convergence, broker-404→EXPIRED handling. This is the doc that `docs/archived-docs/runtime/order-state-machine.md` points to as canonical (though at the wrong path, per earlier stale-ref finding) — content confirms it is indeed the up-to-date successor.
- docs/backend/cli/cli.md — canonical current CLI index (rebuilt by `cli_documentation_drift_audit.md`); lists 12 domains, 85 commands total, per-domain doc links. Self-describes `pyproject.toml` having no `[project.scripts]` alias for `atp`.
- docs/backend/cli/runtime_harness_reference.md — very large (2106 lines) historical operator handbook, self-labeled at the top as superseded by `cli.md` for current inventory ("Total commands: 56 across 12 domains" — now stale, actual is 85 per `cli.md`/drift audit). Per-command tables include refactor recommendations (e.g., flags `backtesting read-controls`/`read-settings`/`seed-fixture`/`seed-controls`/`seed-settings`/`read-portfolio`/`read-dashboard`/`verify-*` as domain-misplaced, matching findings in the CLI domain audits). Retained intentionally as a historical/deeper reference, not contradicting `cli.md`.
- docs/backend/cli/strategy_generation.md — focused CLI reference for `research generate-strategies`/`list-strategy-types`/`list-components` etc.; documents artifact shape, config_hash determinism, grid/random/evolutionary generator determinism guarantees.
- docs/backend/execution/README.md — thin CLI-command index for `execution` domain (5 commands), links to execution.md, execution_policy_simulation_parity.md, broker doc.
- docs/backend/execution/execution.md — current execution domain doc: order flow/state machine, fill/ledger updates, reconciliation (explicitly notes "no freeze or human acknowledgment workflow exists" — a known gap vs the v1 spec), retry idempotency via client_order_id + ambiguous-failure lookup-before-retry logic, two-phase fill-quality persistence (F-04) and out-of-order broker update protection (F-05) — the latter two directly correspond to findings F-04/F-05 in `docs/audits/execution_simulation_audit.md`, suggesting those findings were addressed.
- docs/backend/execution/execution_policy_simulation_parity.md — TASK D-01 implementation doc: simulation now applies same TWAP/VWAP-lite/LIMIT-offset execution policy semantics as live via a shared `IExecutionModel` abstraction (`SimulatedExecutionModel`), resolving the "execution policy not applied in simulation" gap (finding D-01) flagged in `docs/audits/execution_simulation_audit.md`.
- docs/backend/ingestion/README.md — thin CLI-command index for `ingestion` domain (4 commands).
- docs/backend/ingestion/ingestion.md — current ingestion domain doc; explicitly documents simplifications vs original v1 spec: no SLA decision tree (skip/degrade/halt), no 30s/90s deadline windows, no forward-fill, simplified outlier rule (20% deviation vs previous close, not rolling stats), corporate-action continuity validation not enforced, simplified event schema (missing severity/action-classification/dataset-linkage) — consistent with gaps described in `docs/architecture/data-flow.md` and `docs/archived-docs/ingestion/*`.
- docs/backend/observability/README.md — thin CLI-command index for observability-related commands (`operations verify-runtime-soak`, `diagnostics snapshot`, `universe observability-status`).
- docs/backend/observability/alerting.md — alert severity taxonomy (info/warning/critical), Prometheus alert provisioning path, operator alert-response REST endpoints (acknowledge/resolve/snooze/notes), explicit note that alert `recommended_action` is advisory only — API does not auto-trigger destructive controls.
- docs/backend/observability/correlation_conventions.md — short reference: `runtime_job_runs.correlation_id` is the cross-signal correlation key; explicit rule that Prometheus metrics must NOT use `correlation_id`/`run_id`/`job_run_id` as labels (cardinality control).
- docs/backend/observability/instrumentation_inventory.md — large, detailed current telemetry inventory (last updated 5/14/2026): full metrics table, logging/tracing inventory, persistence-backed observability tables, health endpoint inventory, CLI observability entry points, current LGTM/Grafana/OTel docker-compose config. Self-reports several gaps: `ratp_open_orders` and `ratp_incident_events_total` metrics are declared but have no production emitter (test-only), several corporate-action/backfill throughput metrics lack confirmed emitters, and no alert rule files were found at audit time (though `alerting.md`, read separately, describes alert provisioning as if it exists — worth reconciling, though this could reflect the alerting system landing after this inventory's last-updated date).

## docs/backend/ — batch 12 (orchestration, portfolio-governance, research)

- docs/backend/orchestration/ingestion-cycle.md — current (simplified) ingestion cycle doc; explicitly enumerates gaps vs original v1 spec: no staged SLA (30s/90s), no forward-fill, simplified 20%-deviation outlier rule instead of rolling stats, no corporate-action continuity validation, simplified event schema (no severity/action-classification/dataset-linkage) — corroborates `docs/backend/ingestion/ingestion.md` almost verbatim.
- docs/backend/orchestration/scheduler.md — current scheduler domain doc: actual `run_trading_cycle` executes only 5 of the originally-specified 9 steps (no pre-evaluation reconciliation, no scheduler event sequence, freeze logic stubbed/no-op, hardcoded expected symbols with no universe integration).
- docs/backend/orchestration/trading-cycle.md — most detailed current trading-cycle doc; step-by-step actual behavior (readiness is time-based only, not data-validated; risk snapshot computed but not used as an in-cycle blocking gate; reconciliation limited to tracked orders only). Consistent with data-flow.md and scheduler.md's "known gaps" framing.
- docs/backend/portfolio-governance/README.md — thin CLI-command index (5 `backtesting verify-*`/`read-*` commands), links to the portfolio_governance_allocation_audit.md and safety.md.
- docs/backend/research/README.md — CLI-command index for `research`/`strategy` domains (13 commands), links to strategy_registry.md, strategy_generation_engine.md, research_checkpoint_resume.md, simulation/research_execution_paths.md.
- docs/backend/research/advanced_validation_framework.md — TASK-2.4 doc: 6-dimension RobustnessScore framework (walk-forward, MC stability, regime, stress, overfitting, parameter sensitivity) with documented formulas/weights, 7 built-in stress scenarios, 7 overfitting heuristic indicators, Parquet artifact persistence, CLI (`scripts/analyze_validation.py`). Detailed, current, internally consistent.
- docs/backend/research/component_registry.md — current doc for `ComponentRegistry` (indicators/signal_rules/filters/aggregators/exit_rules/sizing component metadata); documents 13 executable indicators, 3 executable signal rules, 4 executable aggregators, and several metadata-only placeholder components (filters/exits/sizing) not yet implemented.
- docs/backend/research/composite_rule_strategy.md — current doc for `CompositeRuleStrategy` declarative composition layer; detailed config schema, deterministic execution order, explainability payload shape, warmup derivation from component metadata. Notes composite strategies don't yet declare `required_persisted_features` (TASK-2.1 hook exists but unused).
- docs/backend/research/market_regime_classification.md — TASK-2.2 doc: deterministic rule-based 5-dimension regime classifier (trend/volatility/liquidity/mean-reversion/risk) producing a persisted, versioned `regime_classification` feature dataset with full schema table; explicitly "No ML." Documents relationship to legacy simpler `regime` feature (both kept, independently versioned).
- docs/backend/research/ml_assisted_research.md — TASK-2.5 doc: "ML-assisted" but explicitly NOT predictive/live ML — deterministic candidate ranking, hierarchical strategy clustering, robustness/overfitting probability estimation, all built from documented weighted formulas over a stable 35-field normalized feature vector. Explicitly states `is_deployable=True` is a human-review flag only, not auto-promotion.
- docs/backend/research/parallel_research_execution.md — TASK-3.2 doc: thread-based (not process/distributed) local parallel execution for independent simulation units; explicitly excludes live/paper trading paths; documents determinism contract (units submitted/aggregated in stable sort-key order) and cache/checkpoint thread-safety via locks.
- docs/backend/research/regime_conditioned_analysis.md — TASK-2.3 doc: post-simulation regime-sliced performance analysis (Sharpe/CAGR/drawdown/trade metrics per regime bucket), exact-timestamp-only joins (explicitly no forward-fill, to avoid look-ahead), `StrategyRegimeProfile` sensitivity scoring. Notes one deferred item: composite-strategy `last_explainability` is not yet persisted to Parquet for regime-level rule analysis.
- docs/backend/research/research.md — ⚠ STALE / internally inconsistent: this is a thin placeholder stating "This domain is not yet implemented... no research workflows, experiment tracking systems, or analysis tools integrated into the platform" — directly contradicted by its own sibling files in the same `docs/backend/research/` folder (research_caching.md, parallel_research_execution.md, advanced_validation_framework.md, ml_assisted_research.md, market_regime_classification.md, regime_conditioned_analysis.md, composite_rule_strategy.md, component_registry.md), all of which describe an extensive, clearly-implemented, versioned (TASK-2.1 through TASK-3.4B+) research pipeline. This file was flagged in `docs/audits/documentation_inventory_audit.md` as a "thin placeholder" needing replacement — confirmed still unresolved.
- docs/backend/research/research_caching.md — current doc for exact-match-only research caching (`StrategyGenerationCache`, `SimulationResultCache`); documents full `SimulationCacheKey` field list (14 fields incl. dataset/universe version, price basis, fill policy, feature_versions_hash), 13 named cache-invalidation reasons, JSON persistence, thread-safety via locks, explicitly no TTL/no distributed cache (deferred).
- docs/backend/research/research_checkpoint_resume.md — current doc for research-only checkpoint/resume (explicitly does not apply to live/paper trading or scheduler retries); documents 4-step restart-plan decision order (exact cache hit → completed checkpoint → rerun failed/missing → identity-mismatch fail) and the 3 `research` CLI commands that operate on it.

## docs/backend/ — batch 13 (research cont'd, runtime, safety, simulation, storage-lineage, universe)

- docs/backend/research/strategy.md — current `strategy` domain doc: decision flow (Market Data→Strategy Evaluation→Signal→OrderIntent), intended IDLE→SIGNALLED→PENDING→IN_POSITION→EXIT_PENDING→COOLDOWN→IDLE lifecycle, current-behavior/limitations split honestly documenting that fill events don't reliably drive IN_POSITION transitions and rejections don't reliably drive COOLDOWN. Consistent framing with `docs/archived-docs/runtime/strategy-state-machine.md`.
- docs/backend/research/strategy_generation_engine.md — current doc for `StrategyGenerationEngine` (grid/random/evolutionary candidate generation, `ParameterSpaceResolver`, dedup by `config_hash()`, composite-rule template generation). References `docs/cli/strategy_generation.md` — ⚠ STALE: actual path is `docs/backend/cli/strategy_generation.md` (`docs/cli/` does not exist; matches the pre-migration path recorded in `documentation_tree_migration_summary.md`).
- docs/backend/research/strategy_registry.md — detailed, current doc for `strategy.registry` (StrategyDefinition fields, ParameterSpec, per-strategy schemas, family/warmup tables, registration lifecycle, registry API, `strategy.catalog` backward-compat shim). ⚠ STALE: twice references `docs/architecture/indicator_vs_feature_architecture.md`, `docs/architecture/component_registry.md`, and `docs/architecture/feature_dependency_resolution.md` — all three actually live under `docs/backend/storage-lineage/` and `docs/backend/research/` respectively per the migration summary; `docs/architecture/` only contains `data-flow.md`, `layering.md`, `system-overview.md` (verified via Glob).
- docs/backend/runtime/README.md — thin CLI-command index for `runtime` domain (10 commands incl. `run-cycle`, `trigger-job`, `soak-loop backtest/paper/research`, `replay`, `replay-debug`, `replay-ingestion`).
- docs/backend/runtime/failure-modes.md — current failure-mode doc across ingestion-readiness/order-submission/state-machine/reconciliation/freeze/kill-switch/scheduler paths; candid "intended vs current" framing (freeze is stubbed — `freeze_trading()` just prints, `is_trading_frozen()` always `False`). ⚠ STALE (or at least contradicted): states "kill switch state is in-memory" and "no persistent or external kill-switch mechanism" — but `src/autonomous_trading_platform/storage/sor/repositories/core/kill_switch_state_repository.py` and `storage/sor/models/kill_switch_state.py` exist (verified via Grep), and the `docs/audits/.../safety_cli_safety_py_domain_audit.md` (covered in an earlier batch) independently documents a DB-backed `kill_switch_state` singleton row. This claim looks out of date relative to the code and to a sibling audit doc.
- docs/backend/safety/README.md — thin CLI-command index for `safety` domain (5 commands: arm-live/disarm-live/enable-kill-switch/disable-kill-switch/gate-status).
- docs/backend/safety/safety.md — current `safety` domain doc (environment model, live-enablement gating, idempotency, caps/throttles, broker allowlist, shadow mode). ⚠ STALE (same issue as failure-modes.md): repeats "Kill switch is in-memory (not external or persistent)" / "Runtime gate and kill switch are in-memory only" — contradicted by the persisted `kill_switch_state` repository/model found in `src/`.
- docs/backend/simulation/README.md — thin CLI-command index for `backtesting`-domain commands (13 commands); flags `backtesting run` with an explicit "TODO: manually verify whether this path remains implemented or placeholder" note.
- docs/backend/simulation/backtesting.md — ⚠ HEAVILY STALE: thin placeholder stating "This domain is not yet implemented. There is no backtesting engine, fill model, or cost model integrated into the system" — directly contradicted by its own sibling `docs/backend/simulation/research_execution_paths.md` (below), which documents multiple working, classified backtest/replay execution paths. `docs/implementation-summaries/documentation_tree_migration_summary.md`'s own follow-up recommendations explicitly flag this file (alongside `research.md`) as a placeholder still needing expansion, confirming this is a known, still-unresolved gap rather than a one-off finding.
- docs/backend/simulation/research_execution_paths.md — dated 2026-05-18; authoritative, detailed classification of 7 backtest/replay code paths (canonical_research × 3, canonical_replay × 1, integration_replay/debug_demo × 2, legacy × 1) with entry-point maps, fill-semantics and persistence-behavior comparison tables, and a deferred-work list (TASK-0.2..0.6, e.g. wiring `backtesting run` to `BacktestTradingCycleOrchestrator`, which `docs/backend/simulation/README.md`'s TODO note above independently corroborates is still unresolved). ⚠ STALE: self-references "See also `docs/architecture/research_strategy_audit.md`" — actual current path is `docs/audits/agent-findings/research_strategy_audit.md` (moved per the migration summary; `docs/architecture/` does not contain this file).
- docs/backend/storage-lineage/README.md — thin CLI-command index (1 command: `features run-pipeline`).
- docs/backend/storage-lineage/feature_dependency_resolution.md — detailed, current TASK-2.1 doc: full resolution flow diagram (`SimulationRunner.run()` → `FeatureDependencyResolverService.resolve()` → `FeatureDatasetVersionsRepository.find_for_simulation()` → `SimulationWindowLoader.load_window()` → `StrategyContext.features`), `FEATURE_DATASETS_BY_NAME` table, lineage-validation checks, warmup-resolution comparison table, TASK-2.2 `regime_classification` integration note. Internally consistent with `strategy_registry.md` and `indicator_vs_feature_architecture.md`'s description of the same flow.
- docs/backend/storage-lineage/indicator_vs_feature_architecture.md — current TASK-1.3 doc defining the strategy-indicator vs persisted-feature boundary; detailed overlap-audit table (which indicator/feature pairs are duplicated-and-tested vs intentionally-different vs indicator-only vs feature-only). ⚠ STALE: also references "`docs/architecture/feature_dependency_resolution.md`" for the full flow — same wrong pre-migration path as `strategy_registry.md` above; actual location is the sibling file in this same folder.
- docs/backend/storage-lineage/storage.md — current `storage` domain doc, but describes a notably earlier/simpler state than other current docs in the tree: "No DatasetVersion model or registry exists," "universe_id is randomly generated," audit log "not append-only," no hash chaining. ⚠ Possibly stale relative to siblings: this reads like an earlier-generation domain doc that wasn't updated alongside the richer universe/dataset-versioning system documented in `docs/backend/universe/README.md`'s 28-command CLI surface (candidate-generate, propose-rebalance, rotate, rollback, compare-universes) and `docs/backend/storage-lineage/feature_dependency_resolution.md`'s `FeatureDatasetVersionsRepository` — worth a manual recheck against current ORM models rather than treating this doc's "Limitations" section as current fact.
- docs/backend/storage-lineage/universe.md — current `universe` domain doc; same pattern as storage.md — describes "universe_id is randomly generated (not deterministic)," "snapshots may be updated or overwritten (not strictly immutable)," "lifecycle handling is incomplete." ⚠ Possibly stale relative to the `docs/backend/universe/README.md` CLI surface (28 commands including deterministic `candidate-generate` with price/ADDV/size filters, `rotate`/`rollback`/`rotation-history`/`compare-universes`) and to `docs/audits/agent-findings/cli separate domain audits/universe_cli_domain_audit.md`'s description of a mature versioned-rotation system — same caveat as storage.md.
- docs/backend/universe/README.md — thin CLI-command index for `universe` domain (28 commands — the largest single CLI surface in the repo), links to storage.md/universe.md/scheduler.md/cli.md, and carries its own explicit TODOs ("manually verify which mutating universe commands require approval," "add examples after verifying against a seeded local database").

## docs/frontend, docs/operations, docs/platform, docs/implementation-summaries, docs/templates — batch 14 (final batch)

- docs/frontend/claude_frontend_previous_story.md — historical Claude-context doc (predecessor of root `CLAUDE.md`'s Frontend section) for the frontend build: folder structure, design tokens, Zustand store shape, "What NOT to do" list. Consistent with current root `CLAUDE.md` frontend guidance; kept as frontend history per the migration summary.
- docs/frontend/vite_readme.md — stock Vite React+TS template README (ESLint/React Compiler boilerplate); explicitly filed as "frontend history," not project-specific documentation.
- docs/frontend_audit_and_roadmap.md — dated 2026-06-04; large, detailed frontend-vs-backend gap audit. Catalogs current mock-only frontend state per page, a full current backend REST surface (portfolio/strategy/drawdown-governance/governance-audit/controls/metric-lineage/shadow-validation/operations/settings/audit endpoints), and a phased wiring roadmap (Phase 1-4). Verified `frontend/src/pages/` currently contains exactly the 6 files this doc lists (Dashboard, Portfolio, StrategyLab, Controls, Settings, ExperimentLab.tsx) — no drift found. Internally consistent with CLAUDE.md's "frontend is a static mockup" framing and with the `docs/audits/agent-findings/platform_cli_orchestration_integration_audit.md` finding (covered in an earlier batch) that several operator controls exist only as REST routes with no CLI or UI surface.
- docs/implementation-summaries/documentation_tree_migration_summary.md — the ground-truth record of the `docs/architecture/*.md` → `docs/backend/**` migration that explains nearly every stale cross-reference found throughout this audit (docs still saying `docs/architecture/component_registry.md`, `docs/architecture/feature_dependency_resolution.md`, `docs/architecture/research_strategy_audit.md`, `docs/cli/strategy_generation.md`, etc.). Its own "Follow-Up Recommendations" section explicitly names `docs/backend/research/research.md` and `docs/backend/simulation/backtesting.md` as thin placeholders still needing expansion — confirming both findings above are known, tracked, and still unresolved as of this audit. No stale references found within the file itself (it is itself the historical record).
- docs/operations/debugging/README.md — thin, self-labeled "Status: DRAFT" CLI debugging map (runtime/ingestion/reconciliation/local-verification/replay entry points), links to `cli.md`, `failure-modes.md`, `execution.md`, `instrumentation_inventory.md` — all verified present.
- docs/operations/debugging/alembic_commands.md — short PowerShell command cheat-sheet for diagnosing Alembic/DB issues (`alembic current`, import sanity checks, `docker exec ... psql`). No stale paths; references `ratp_postgres` container name and `.\infra\db\alembic.ini`, consistent with CLAUDE.md's documented Alembic config path.
- docs/operations/runbooks/README.md — thin, self-labeled "Status: DRAFT" CLI runbook index (runtime verification, paper checks, broker reconciliation, ingestion, research replay, universe ops); links to `cli.md`, `runtime/README.md`, `execution/README.md`, `ingestion/README.md`, `observability/README.md` — all verified present.
- docs/platform/platform-replay-domain-hook-audit.md — dated 2026-06-03; large per-domain audit (17 CLI domains) of what's needed to build a `PlatformReplayContext`-driven platform-wide replay orchestrator; documents that `platform backtest plan/run/inspect/report` were ALL `"not_implemented"` stubs at time of writing, with a P0/P1/P2 roadmap. Cross-references `docs/platform_backtest_output_audit.md` (below), which describes the *result* of this plan having since landed (a working `PlatformBacktestRunner` with `PlatformBacktestArtifact` bundle) — i.e., this audit's P0 items (define `PlatformReplayContext`/`DomainReplayResult`, implement `PlatformBacktestRunner`, wire `run_trading_cycle_at_timestamp()` etc.) appear to have been completed since this doc was written, making it a historical planning doc rather than current state. Worth flagging as *not itself contradictory*, just superseded-in-part by later work — no broken paths found.
- docs/platform/settings_reference.md — current, detailed reference distinguishing code-default operator settings (`OperatorSettingsRepository.get_or_create_default()`) from replay-fixture YAML overrides (`fixtures/platform/replays/long/full_year_demo.yaml` → `initial_state.settings`); full per-field table (patchable-from-YAML vs code-only) covering drawdown/allocation/governance/notification settings, plus a "where other thresholds live" table (promotion_rules, drawdown_governance_ladder_state, kill_switch_state DB tables). Notably states "Kill switch state | `kill_switch_state` DB table (singleton) — do not edit directly" — this directly corroborates that kill-switch state IS persisted in the DB, reinforcing the ⚠ STALE finding against `docs/backend/safety/safety.md` and `docs/backend/runtime/failure-modes.md` above (both claim in-memory-only kill switch).
- docs/platform_backtest_output_audit.md — deep audit of what `PlatformBacktestArtifact` (the `atp platform backtest run` output bundle) currently contains vs. what's needed for real performance review (Sharpe/Sortino/max-drawdown/CAGR are computed by `PortfolioAnalyticsService.get_performance()` but never called from the artifact-building hook; benchmark comparison data is entirely absent). Confirms the platform backtest runner described as not-yet-built in `platform-replay-domain-hook-audit.md` above is now implemented and produces a real (if metrics-incomplete) artifact — useful corroboration that that P0 roadmap substantially landed. No stale paths found; all referenced source files (`contracts/runtime/platform_replay.py`, `cli/commands/platform.py`, `application/services/platform_replay/*_hooks.py`) match the layered-architecture directories CLAUDE.md describes.
- docs/templates/sor_template.md — generic Python repository-class template/boilerplate (`<ModelName>Repository` with `get_by_id`/`insert`/`upsert`/`delete_by_id`) for scaffolding new SoR repositories; not a prose doc, no paths to verify.

---

## Docs highlights

Most interviewer-worthy documents in the tree, by path:

- `docs/backend/simulation/research_execution_paths.md` — the single clearest "here's what's real vs. legacy vs. debug-only" map in the whole repo; explains why there are 7 different backtest/replay code paths and which one to use for what.
- `docs/platform_backtest_output_audit.md` + `docs/platform/platform-replay-domain-hook-audit.md` — paired before/after: the second documents the P0 plan for a platform-wide `PlatformReplayContext` replay engine, the first audits the resulting `PlatformBacktestArtifact` and shows exactly which performance metrics (Sharpe, max drawdown over the full run, benchmark comparisons) are still missing from it.
- `docs/frontend_audit_and_roadmap.md` — comprehensive current-REST-surface-vs-mock-frontend gap analysis with a phased wiring roadmap; the best single artifact for understanding how far ahead the backend has grown of the (intentionally static-mockup) frontend.
- `docs/backend/observability/instrumentation_inventory.md` — full current telemetry inventory (metrics, tracing, persistence-backed observability tables, Grafana/OTel docker-compose config), self-reporting its own gaps (metrics declared but never emitted).
- `docs/audits/agent-findings/cli_documentation_drift_audit.md` — ground-truth CLI census (85 commands / 12 domains) that rebuilt `docs/backend/cli/cli.md` and spawned the 17-file "cli separate domain audits" set, several of which found real bugs (broken `execution reconcile-open-orders` wiring, `runtime run-cycle --timestamp` silently ignored, `features run-pipeline --include-*` flags that can't actually disable anything).
- `docs/backend/storage-lineage/feature_dependency_resolution.md` — clean, precise TASK-2.1 flow doc; a good example of documentation quality at its best in this repo.
- `docs/archived-docs/` (64 files) — the original locked v1 spec canon (contracts, invariants, safety doctrine, runtime lifecycle); useful as a "what we originally promised" baseline against which nearly every `docs/backend/*` "Current Behavior / Limitations" doc measures itself.
- `docs/platform/settings_reference.md` — the one doc that actually distinguishes "what changes if I edit this YAML field" from "what's hardcoded" — rare and valuable operational precision.

## Stale-reference table

Consolidating all staleness findings from this audit (both this session and the prior session's entries):

| Doc | Issue |
|---|---|
| `docs/README.md` | References `docs/roadmaps/` and `docs/unsorted/` — neither exists. |
| `CLAUDE.md` (root) | Calls out `docs/architecture/v1-boundaries.md`, `docs/architecture/safety-doctrine.md`, `docs/architecture/invariants.md` as canonical — all three actually live at `docs/archived-docs/*.md`, not `docs/architecture/*.md`. |
| `docs/archived-docs/runtime/order-state-machine.md` | Points to canonical successor at `docs/architecture/broker_event_stream_and_order_lifecycle.md` — actual path is `docs/backend/broker/broker_event_stream_and_order_lifecycle.md`. |
| `docs/archived-docs/storage/index.md` | References sibling files `dataset-versioning.md`/`universe-versioning.md` (actual: `dataset-version.md`/`universe-version.md`) and a top-level `docs/contracts/` that doesn't exist. |
| `docs/backend/research/strategy_registry.md` | References `docs/architecture/component_registry.md`, `docs/architecture/indicator_vs_feature_architecture.md`, `docs/architecture/feature_dependency_resolution.md` — all pre-migration paths; actual locations are `docs/backend/research/component_registry.md` and `docs/backend/storage-lineage/*.md`. |
| `docs/backend/research/strategy_generation_engine.md` | References `docs/cli/strategy_generation.md` — actual path `docs/backend/cli/strategy_generation.md`. |
| `docs/backend/storage-lineage/indicator_vs_feature_architecture.md` | References `docs/architecture/feature_dependency_resolution.md` — actual path is the sibling file in the same folder, `docs/backend/storage-lineage/feature_dependency_resolution.md`. |
| `docs/backend/simulation/research_execution_paths.md` | References `docs/architecture/research_strategy_audit.md` — actual path `docs/audits/agent-findings/research_strategy_audit.md`. |
| `docs/backend/research/research.md` | Thin placeholder ("domain not yet implemented") flatly contradicted by 8 sibling files describing an extensive implemented research pipeline; confirmed still-unresolved by `documentation_tree_migration_summary.md`'s own follow-up list. |
| `docs/backend/simulation/backtesting.md` | Same pattern — "not yet implemented" placeholder contradicted by `research_execution_paths.md` in the same folder; also confirmed still-unresolved in the migration summary's follow-ups. |
| `docs/backend/safety/safety.md` + `docs/backend/runtime/failure-modes.md` | Both claim kill switch / runtime gate state is "in-memory only, not persistent or external" — contradicted by `storage/sor/repositories/core/kill_switch_state_repository.py`, `storage/sor/models/kill_switch_state.py` (verified in source), by the `safety_cli_safety_py_domain_audit.md` finding a DB-backed singleton row, and by `docs/platform/settings_reference.md` explicitly documenting the `kill_switch_state` DB table. |
| `docs/backend/storage-lineage/storage.md` + `docs/backend/storage-lineage/universe.md` | Describe an earlier/simpler system state (random `universe_id`, no `DatasetVersion` model, non-immutable snapshots) that reads inconsistently against the much richer universe/versioning system documented in `docs/backend/universe/README.md` (28 CLI commands incl. deterministic candidate generation, rotation, rollback) — likely stale, not re-verified against current ORM models. |
| `docs/audits/agent-findings/portfolio_governance_allocation_audit.md` | FINDING-01 (no auto-demotion) and FINDING-03 (kill switch in-memory, no DB record) are superseded by later-dated sibling audits documenting a working `AutoDemotionService` and a `KillSwitchStateRepository`. |
| `docs/audits/documentation_inventory_audit.md` | Describes a doc tree (`docs/domains/`, `docs/orchestration/`, `docs/cli/`, `docs/interfaces/cli.md`, etc.) that no longer exists anywhere in the repo — entirely pre-migration; superseded by `documentation_tree_migration_summary.md`. |
| `mkdocs.yml` | Nav section (`nav:` in the repo-root `mkdocs.yml`) references paths like `index.md`, `contracts/index.md`, `runtime/index.md`, `safety/index.md`, `storage/index.md`, `universe/index.md`, `paper-validation/index.md`, `vertical-slice/index.md`, `research/index.md` — none of these resolve under the default `docs/` docs_dir; the actual files all live one level deeper, under `docs/archived-docs/**`. An `mkdocs build`/`mkdocs serve` would 404 on every nav entry. The nav also only covers the old archived-docs v1-spec tree and omits `docs/backend/`, `docs/audits/`, `docs/architecture/`, `docs/operations/`, `docs/platform/`, `docs/frontend/`, `docs/templates/`, and `docs/implementation-summaries/` entirely — i.e., roughly 86 of 150 files (everything outside `docs/archived-docs/`) have no nav entry at all. |

## docs/archived-docs/ canon list — Phase 0-6 check

No numbered "Phase 0" through "Phase 6" canon list exists anywhere under `docs/archived-docs/` (searched all 64 files). What actually exists is:
- `docs/archived-docs/index.md` — organizes the v1 canon into 8 named (not numbered 0-6) subsystems: Contracts, Ingestion, Research, Runtime, Safety, Universe, Vertical Slice, Paper Validation — plus 5 "Core Docs" (`v1-boundaries.md`, `safety-doctrine.md`, `compile-vs-runtime.md`, `decisions.md`, `invariants.md`).
- `docs/archived-docs/decisions.md` — an ADR-lite log (D-001 through D-005), not phase-numbered.
- The only actual "Phase N" self-labels found in the tree are **Phase 8** (`docs/archived-docs/vertical-slice/*.md`, 3 files) and **Phase 9** (`docs/archived-docs/paper-validation/*.md`, 3 files) — no Phase 0-6 docs exist under `archived-docs`. If a "Phase 0-6" canon list was expected, it does not exist in this repo as written; the closest analog is the 8-subsystem index above.

## mkdocs.yml nav-vs-files check

`mkdocs.yml` (repo root) nav lists 10 entries, all pointing at a flat `docs/` layout (`contracts/index.md`, `ingestion/index.md`, `runtime/index.md`, `safety/index.md`, `storage/index.md`, `universe/index.md`, `paper-validation/index.md`, `vertical-slice/index.md`, `research/index.md`, plus `index.md`). None of these paths exist relative to `docs/` — the real files are one directory deeper, under `docs/archived-docs/{contracts,ingestion,runtime,safety,storage,universe,paper-validation,vertical-slice,research}/index.md`. The nav predates the `docs/archived-docs/` rename and was never updated; it is fully broken and covers only the original 64-file v1-spec canon at that — the other ~86 files (all of `docs/backend/`, `docs/audits/`, `docs/architecture/`, `docs/operations/`, `docs/platform/`, `docs/frontend/`, `docs/templates/`, `docs/implementation-summaries/`, and the 4 root-level docs files) have zero nav coverage.

## Coverage: opened 150 of 150
